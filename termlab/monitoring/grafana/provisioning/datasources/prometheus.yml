# Grafana datasource provisioning: makes Prometheus the default datasource on startup so
# no clicking is needed. The uid "prometheus" is referenced by every dashboard panel.
apiVersion: 1
datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
